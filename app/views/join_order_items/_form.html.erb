<%= form_with(model: join_order_item, local: true) do |form| %>
  <% if join_order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(join_order_item.errors.count, "error") %> prohibited this join_order_item from being saved:</h2>

      <ul>
      <% join_order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
